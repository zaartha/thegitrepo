# Exercise 05 — Branching

## Tasks

1. `git branch feature-about`
2. `git switch feature-about`
3. Create `about.html`, commit it
4. `git switch main` — notice `about.html` is gone
5. `git switch -c feature-contact`
6. `git log --graph --oneline --all`
