# Exercise 01 — Multiple Remotes

1. Clone any public GitHub repo
2. `git remote add upstream <original-url>`
3. `git remote -v` — confirm both `origin` and `upstream`
4. `git fetch upstream`
5. `git log upstream/main --oneline`
6. `git fetch --prune` — clean up stale refs
