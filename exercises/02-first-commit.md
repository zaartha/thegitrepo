# Exercise 02 — First Commit

## Goal
Initialize a repo and make your first commit.

## Tasks

1. `mkdir my-first-repo && cd my-first-repo && git init`
2. `echo "# My First Repo" > README.md`
3. `git add README.md`
4. `git commit -m "docs: add README"`
5. `git log --oneline`
