# Exercise 04 — Reading History

## Tasks

1. Make at least 4 commits with meaningful messages
2. Run and compare:
   ```bash
   git log
   git log --oneline
   git log --graph --oneline --all
   ```
3. `git show <hash>` on your latest commit
4. Filter: `git log --grep="docs"`
