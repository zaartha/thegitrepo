# Exercise 07 — Tags

## Tasks

1. `git tag -a v1.0.0 -m "Initial release"`
2. `git tag`
3. `git show v1.0.0`
4. Tag a past commit: `git tag v0.9.0 <hash>`
5. `git push origin v1.0.0`
6. `git push origin --tags`
